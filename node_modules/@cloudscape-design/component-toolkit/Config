package.AWS-UI-Component-Toolkit = {
  interfaces = (1.0);

  build-system = npm-pretty-much;
  build-tools = {
      1.0 = {
          NpmPrettyMuch = 1.1;
          NodeJS = default;
      };
  };

  dependencies = {
      1.0 = {

      };
  };

  test-dependencies = {
      1.0 = {
          AWS-UI-ReleaseTools = 3.0;
      };
  };
};
