import { CancelableEventHandler } from '../internal/events';
import { BreadcrumbGroupProps } from './interfaces';
interface FullCollapsedDropdownProps {
  items: ReadonlyArray<BreadcrumbGroupProps.Item>;
  onItemClick: CancelableEventHandler<{
    id: string;
  }>;
  onItemFollow: CancelableEventHandler<{
    id: string;
  }>;
}
export declare const AllItemsDropdown: ({
  items,
  onItemClick,
  onItemFollow
}: FullCollapsedDropdownProps) => JSX.Element;
export {};