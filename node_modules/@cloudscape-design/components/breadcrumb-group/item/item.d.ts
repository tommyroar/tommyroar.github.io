import { BreadcrumbGroupProps, BreadcrumbItemProps } from '../interfaces';
export declare function BreadcrumbItem<T extends BreadcrumbGroupProps.Item>({
  item,
  itemIndex,
  totalCount,
  onClick,
  onFollow,
  isGhost,
  isTruncated
}: BreadcrumbItemProps<T>): JSX.Element;