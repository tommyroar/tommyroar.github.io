import React from 'react';
import { InternalButtonProps } from '../../button/internal';
interface ActionsWrapperProps {
  className: string;
  testUtilClasses: {
    actionSlot: string;
    actionButton: string;
  };
  action: React.ReactNode;
  discoveredActions: Array<React.ReactNode>;
  buttonText: React.ReactNode;
  wrappedClass?: string;
  containerWidth?: number;
  onButtonClick: InternalButtonProps['onClick'];
}
export declare const ActionsWrapper: ({
  className,
  testUtilClasses,
  action,
  discoveredActions,
  buttonText,
  wrappedClass,
  containerWidth,
  onButtonClick
}: ActionsWrapperProps) => JSX.Element | null;
export {};