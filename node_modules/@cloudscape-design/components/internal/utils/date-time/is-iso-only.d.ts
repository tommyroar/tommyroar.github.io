/**
 * Checks if ISO date-string is date-only.
 */
export declare function isIsoDateOnly(dateString: string): boolean;
/**
 * Checks if ISO date-string is month-only.
 */
export declare function isIsoMonthOnly(dateString: string): boolean;