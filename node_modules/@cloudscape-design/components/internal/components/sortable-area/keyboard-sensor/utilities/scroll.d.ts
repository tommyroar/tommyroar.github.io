import { Coordinates } from '@dnd-kit/utilities';
export declare function applyScroll({
  currentCoordinates,
  direction,
  newCoordinates,
  scrollableAncestors
}: {
  currentCoordinates: Coordinates;
  direction: 'up' | 'down';
  newCoordinates: Coordinates;
  scrollableAncestors: Element[];
}): boolean;