import { ResizableDrawerProps } from './interfaces';
export declare const ResizableDrawer: ({
  onResize,
  maxWidth,
  minWidth,
  refs,
  activeDrawer,
  toolsContent,
  ...props
}: ResizableDrawerProps) => JSX.Element;